{
  "name": "ocp-provizie",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cp index.html login.html login.js ."
    "build": "npm install"
  },
  "dependencies": {
    "express": "^4.17.1",
    "serverless-http": "^2.7.0",
    "bcrypt": "^5.0.1"
  }
}
